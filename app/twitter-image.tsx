// Same card as the Open Graph image — Next resolves `twitter:image` from this
// file convention rather than falling back to `opengraph-image`.
export { default, alt, size, contentType } from "./opengraph-image";
